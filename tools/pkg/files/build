#!/usr/bin/env bash

set -e

GIT_REF=$1
REVISION=${2:-1}

if [ x"$GIT_REF" == x"" ]; then
    echo "$0: which version to build?"
    exit 1
fi

cd /buildfiles
bash ./script.sh ${GIT_REF} ${REVISION} "https://github.com/esl/MongooseIM" "21.3" "22.3"
cp -i /root/rpm/mongooseim*.??? /packages/
