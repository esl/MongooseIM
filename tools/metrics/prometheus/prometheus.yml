global:
  scrape_interval: 5s
  scrape_timeout: 5s

rule_files:
  - alert.yml

scrape_configs:
  - job_name: mongooseim
    static_configs:
      - targets: ['localhost:4445']
  - job_name: prometheus
    static_configs:
      - targets:
          - 'localhost:9090'
